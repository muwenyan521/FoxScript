Class Console 
	func WriteLine(val)
		' 传递一个参数，输出该参数的值，无返回值

		'使用内置函数输出值
		print(val)
	endfunc
EndClass

Console.WriteLine("Hello World!")

Class Person
	Dim Name = ""
	Dim Age = 0
EndClass

Console.WriteLine(Person.Name)
Console.WriteLine(Person.Age)
Person.Name = "小明"
Person.Age = 11
Console.WriteLine(Person.Name)
Console.WriteLine(Person.Age)

input()